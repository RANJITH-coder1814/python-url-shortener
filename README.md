# python-url-shortener
A simple Python-based URL shortener that converts long URLs into short, easy-to-share links using random key generation and file-based storage.

A simple Python-based URL shortener that converts long URLs into short, easy-to-share links using random key generation and file-based storage.

---

##  Features
- Shorten long URLs
- Retrieve original URLs
- Random short-code generation
- File-based data storage
- Beginner-friendly CLI

---

## 🛠️ Technologies Used
- Python 3
- JSON File Storage

---
   ```bash
   git clone https://github.com/yourusername/python-url-shortener.git
